validation filetype images via multer

check and sanitise user input

how does a method in a component work? -> see error from the function that sanitises the data

resize the menu for mobile (hamburger menu)

<comment-component></comment-component>


v-bind:id="imageId" v-if="modalOnOff"


window.onscroll = () => {
        let bottomOfWindow = document.documentElement.scrollTop + window.innerHeight === document.documentElement.offsetHeight;
        if (bottomOfWindow) {
          axios.get(`https://randomuser.me/api/`).then(response => {
            this.users.push(response.data.results[0]);
          });
        }
      }


function checkScroll() {
timer = setTimeout(function () {
            if (document.documentElement.scrollTop + window.innerHeight >= document.documentElement.offsetHeight) {
                // fetch request here;
            } else {
                checkScroll();
            }
        }, 750);

}



function checkScroll() {
setInterval(() => {
            if(window.scrollMaxY - window.scrollY > 1000){
                 //fetch request;
            }
       }, 2000);

}



         for (let item of this.images) {
                if (this.lowestImageId === item.id) {
                    console.log("lowest image id is among the images");
                    return;
                } else {
                    console.log("else of the check");
                    return window.onscroll;
                }
            }

